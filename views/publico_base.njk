<!doctype html>
<html>
    <head>
        <title>{% block titulo %} {% endblock %}</title>
        <link rel="stylesheet" href="/css/bootstrap.min.css"/>
    </head>
    <body>
        <div class="container">
            <div class="alert alert-secondary">
                <a class="btn btn-dark" href="/">Inicio</a>
                &nbsp;&nbsp;
                <a class="btn btn-dark" href="/juegos">Administración</a>
                &nbsp;&nbsp;
                {% if (session and session.usuario) %}
                    <a class="btn btn-dark" href="/auth/logout">Logout</a>
                {% else %}
                    <a class="btn btn-dark" href="/auth/login">Login</a>
                {% endif %}
            </div>
            {% block contenido %}
            {% endblock %}
        </div>
    </body>
</html>